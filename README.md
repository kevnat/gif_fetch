# gif_fetch

This little toy hits the Giphy API via AJAX to display a set of results in the page using jQuery. Users can add new 'Reaction' button by entering a search term, which also happens through jQuery DOM manipulation.
